---
# yaml-language-server: $schema=https://raw.githubusercontent.com/marcocondrache/kubef/refs/heads/main/schemas/schema.json
groups:
    test:
        - alias: pdf
          namespace: default
          selector:
              type: service
              match: pdf
          ports:
              remote: 8080
              local: 9000
